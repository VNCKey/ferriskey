# 📖 Teoría: 02 - Conceptos

## 1. Variables & Mutabilidad
En Rust las variables son inmutables por defecto. Se declara mutabilidad explícita con `mut`.

## 2. Shadowing (Sombreado)
Re-declarar una variable con `let` permite cambiar el tipo manteniendo el mismo nombre.
